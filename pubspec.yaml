name: flutter_sensors_filter_example
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  sensors: ^2.0.0
  web_socket_channel: ^2.1.0
  cupertino_icons: ^1.0.2
  flutter_application_1: ^0.0.2
  socket_io_client: ^2.0.3+1

  
dev_dependencies:
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true